# 安全政策

## 支持范围

安全修复以 `main` 分支和 GitHub Releases 中最新正式版为准。历史测试版与旧二进制不单独维护。

## 报告漏洞

请优先在仓库的 **Security → Advisories → Report a vulnerability** 使用 GitHub 私密漏洞报告。若该入口暂时不可用，请创建一个不含利用细节、密钥、IP、日志或配置的简短 Issue，请求仓库维护者开启私密沟通；维护者回应前不要公开技术细节。

报告中请说明受影响版本、触发条件、可能影响和可重复的最小步骤。所有令牌、共享密钥、证书私钥、Webhook、真实矿机 IP 和 VPS 登录资料必须替换为虚构值。

维护者应在收到报告后 7 天内确认，并在确认影响后提供修复计划。修复发布前会运行仓库测试、安全回归、升级和回滚检查。

## 非敏感问题

普通功能错误、文档问题和不包含安全影响的改进建议可以直接使用公开 Issue。
