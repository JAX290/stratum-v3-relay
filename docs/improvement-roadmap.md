# 后续更新计划与进度

本文是本项目后续开发计划的唯一记录。README 只说明当前已经实现的功能；尚未实现、正在实现和已经完成的改进都在这里维护。

最后更新：2026-09-21

## 状态说明和维护规则

| 状态 | 含义 |
| --- | --- |
| ✅ 已完成 | 功能已经合入 `main`，升级后可用，并完成必要测试和文档 |
| 🟡 进行中 | 已开始设计或开发，但不能视为生产可用 |
| ⬜ 计划中 | 已确认需要实现，尚未开始 |
| ⏸ 暂缓 | 保留需求，当前阶段不实施 |

每次开始或完成一项工作时，都必须更新本文件：

1. 使用固定编号，避免讨论中名称变化后重复立项。
2. 开始开发时将状态改为“进行中”，写明本次范围。
3. 合入 `main` 后改为“已完成”，补充完成版本、日期和验证结果。
4. 如果只完成一部分，保留“进行中”，在进展栏明确已经完成和仍未完成的内容。
5. 新需求先归入“VPS 端”或“Windows 客户端”，再安排优先级和实施顺序。
6. 自动修复类功能必须同时具备：执行前说明、配置备份、执行后验证、失败回退和连续失败停止机制。

优先级含义：P1 直接影响稳定性或非技术管理员处理故障；P2 明显改善维护效率；P3 属于长期体验和架构优化。

## VPS 端

### 已完成基础

| 编号 | 状态 | 已实现内容 | 当前结果 |
| --- | --- | --- | --- |
| VPS-D01 | ✅ 已完成 | 运维问题中心 | 汇总核心服务状态和最近 24 小时日志，按中文原因和建议展示；技术详情默认折叠 |
| VPS-D02 | ✅ 已完成 | HTTPS 只读值守面板 | 使用独立账号密码，无需 Tailscale；显示脱敏运行信息，不提供修改接口 |
| VPS-D03 | ✅ 已完成 | 配置并发和原子保存 | 跨进程文件锁、配置版本号和历史备份避免线路、试切和同步操作互相覆盖 |
| VPS-D04 | ✅ 已完成 | 双 VPS 即时同步 | 修改线路后等待对端确认，显示成功、失败原因和待重试任务；使用节点序号避免时钟漂移 |
| VPS-D05 | ✅ 已完成 | 服务隔离和自动恢复 | 公网 TLS 服务使用独立低权限账户；systemd 与 VPS 看门狗处理退出和连续无响应 |
| VPS-D06 | ✅ 已完成 | 一键安装、升级和回滚基础 | `deploy.sh` 自动区分安装与升级，保留配置、创建备份并检查服务 |
| VPS-D07 | ✅ 已完成 | 矿场和客户端管理 | 面板可新增、改名和带二次确认删除矿场；共享密钥默认隐藏并记录审计 |
| VPS-D08 | ✅ 已完成 | 管理界面减负 | 活跃矿池优先、未使用矿池折叠、设置分区导航、操作局部刷新并保持页面位置 |
| VPS-D09 | ✅ 已完成 | 多渠道通知 | 企业微信、钉钉、邮箱各支持 0–3 个目标；支持 SMTP 和 VPS 本机直投、页面测试与删除 |
| VPS-D10 | ✅ 已完成 | 管理员完整使用说明 | 说明三种登录方式、页面指标、折叠原理、线路操作和安全边界；面板提供入口 |
| VPS-D11 | ✅ 已完成 | 统一测试和版本清单 | Python、部署脚本、Windows 构建进入 CI；组件版本统一来自 `version.json` |

### 下一阶段计划

| 编号 | 优先级 | 状态 | 计划功能 | 完成标准 |
| --- | --- | --- | --- | --- |
| VPS-P01 | P1 | ⬜ 计划中 | 一键全面诊断 | 一次检查服务、TLS、证书、磁盘、内存、端口、HAProxy、矿池连通性、双 VPS 同步、Windows 心跳、版本和最近 Share；按“正常 / 已恢复 / 可修复 / 需人工”分组 |
| VPS-P02 | P1 | ⬜ 计划中 | 统一问题编号和业务影响 | 每个问题显示影响的矿场、矿机数、持续时间、是否仍在发生以及系统已经尝试的动作 |
| VPS-P03 | P1 | ⬜ 计划中 | 脱敏技术支持包 | 一键导出版本、健康检查、配置摘要和相关日志；导出前扫描并排除密钥、密码、Webhook、证书私钥和完整公网资料 |
| VPS-P04 | P1 | ⬜ 计划中 | 一键安全修复 | 支持重启单个异常服务、重试同步、重新加载已验证配置、重新探测和清理本项目过期日志；修复后自动复查 |
| VPS-P05 | P1 | ⬜ 计划中 | 修复回退与停止保护 | 修复前自动备份，失败时恢复；同一问题连续失败后停止重试，防止矿场反复断线 |
| VPS-P06 | P2 | ⬜ 计划中 | 高风险操作向导 | 线路切换、证书更换、防火墙、DNS 和版本升级采用分步检测，候选方案验证成功后才允许执行 |
| VPS-P07 | P2 | ⬜ 计划中 | 通知闭环 | 告警包含通俗原因、影响范围、问题编号和处理链接；恢复后发送闭环消息，并显示各渠道最近发送结果 |
| VPS-P08 | P2 | ⬜ 计划中 | 通知策略 | 可按严重级别选择渠道，设置免打扰时段，并在全部渠道失败时保留醒目站内提示 |
| VPS-P09 | P2 | ⬜ 计划中 | 双 VPS 统一健康摘要 | 在任一 VPS 查看两台服务器的核心状态、版本、同步延迟和故障差异 |
| VPS-P10 | P2 | ⬜ 计划中 | 每日巡检和稳定性摘要 | 首页显示每日结论和最近 7 天稳定性；全部正常时只显示一句结论 |
| VPS-P11 | P3 | ⬜ 计划中 | 常用线路收藏和变更说明 | 收藏常用矿池地址，切换前显示来源、最近验证结果和预计影响端口 |
| VPS-P12 | P3 | ⬜ 计划中 | 管理面板特权助手拆分 | 把必须使用 root 的配置和服务操作移入最小权限助手，进一步降低 Web 进程权限 |

### VPS 实施顺序

1. `VPS-P01`、`VPS-P02`、`VPS-P03`：先做到问题看得懂、影响说得清、资料可以安全交给技术人员。
2. `VPS-P04`、`VPS-P05`：实现首批低风险修复，并完成验证和回退闭环。
3. `VPS-P06`：把可能影响中转的高风险操作改成向导。
4. `VPS-P07` 至 `VPS-P10`：完善通知、双机管理和周期巡检。
5. `VPS-P11`、`VPS-P12`：完成长期体验与权限架构优化。

## Windows 客户端

### 已完成基础

| 编号 | 状态 | 已实现内容 | 当前结果 |
| --- | --- | --- | --- |
| WIN-D01 | ✅ 已完成 | TLS 加密与身份校验 | 客户端校验证书指纹和独立共享密钥，避免连接到错误 VPS |
| WIN-D02 | ✅ 已完成 | 主、备用 VPS | 新连接在主 VPS 不可用时按顺序尝试备用 VPS；稳定连接不会因主 VPS 恢复被强制迁移 |
| WIN-D03 | ✅ 已完成 | 基础无人值守能力 | 支持开机启动、托盘运行、普通异常自动恢复和恢复次数限制 |
| WIN-D04 | ✅ 已完成 | 一键诊断 | 检查局域网地址、监听端口、中转状态、在线矿机、主备 VPS 和资源，生成可复制报告 |
| WIN-D05 | ✅ 已完成 | 本地敏感配置保护 | 共享密钥使用 Windows DPAPI 保护，本地配置存储已从主界面代码中拆分 |
| WIN-D06 | ✅ 已完成 | 版本与签名发布基础 | 版本来自统一清单；发布流程支持构建、签名校验和 SHA-256 文件 |
| WIN-D07 | ✅ 已完成 | 客户端源码拆分 | 2.2.2 源码已拆出中转核心、统计与历史、网络辅助及各窗口，移除旧配置副本；完整 EXE 与无界面核心各 25 项检查通过。2026-09-21 已合入 main；2.2.2 签名 EXE 已发布（client-v2.2.2，e053f64），附 SHA-256 校验文件 |

### 下一阶段计划

| 编号 | 优先级 | 状态 | 计划功能 | 完成标准 |
| --- | --- | --- | --- | --- |
| WIN-P01 | P1 | 🟡 进行中 | Windows 服务和独立看门狗 | 无需用户登录即可运行；进程退出、完全卡死或电脑重启后恢复，反复失败时停止并说明原因。已实现恢复策略、原子持久化与互斥、服务宿主、独立工作进程及加密配置接口；119 项隔离检查通过。仍待安装迁移、SCM／权限、电脑重启和失败回退实机验收，未发布正式服务版（2026-09-21） |
| WIN-P02 | P1 | ⬜ 计划中 | 一键检查并修复 | 处理程序未启动、配置错误、端口冲突、防火墙缺失、主 VPS 不通但备用可用和版本过旧；普通诊断不请求管理员权限 |
| WIN-P03 | P1 | ⬜ 计划中 | 最后可用配置与自动回退 | 新设置验证失败时恢复上一份已经通过证书、密钥、端口和 VPS 连通性验证的配置 |
| WIN-P04 | P1 | ⬜ 计划中 | 主备切换防抖 | 增加连续失败阈值、恢复观察期和切换冷却，避免网络抖动时来回切换 |
| WIN-P05 | P1 | ⬜ 计划中 | 网络与系统恢复 | 断网恢复、休眠唤醒、网卡切换和局域网 IP 变化后自动重建监听与 VPS 连接，并显示进度 |
| WIN-P06 | P2 | ⬜ 计划中 | 值守模式首页 | 默认只显示总状态、受影响矿机数、“检查并修复”和“联系技术人员”，高级参数移到单独页面 |
| WIN-P07 | P2 | ⬜ 计划中 | 值守员与管理员模式 | 值守员只能日常检查和执行安全修复；端口、证书、密钥与高级设置需要管理员身份 |
| WIN-P08 | P2 | ⬜ 计划中 | 加密接入文件 | 导入后自动填写 VPS、端口、证书指纹和矿场密钥；支持有效期和一次性使用，减少人工复制 |
| WIN-P09 | P2 | ⬜ 计划中 | 换机备份与迁移向导 | 新电脑先完成证书、密钥、端口和 VPS 验证，成功后才提示切换矿机地址 |
| WIN-P10 | P2 | ⬜ 计划中 | 签名自动升级与回退 | 下载后校验发布者、版本和签名，支持分批升级；启动检查失败时恢复上一版本 |
| WIN-P11 | P2 | ⬜ 计划中 | VPS 远程状态与有限操作 | 管理面板显示客户端在线、版本、当前 VPS、最近 Share 和重连次数；只开放诊断、重连和升级等受限动作 |
| WIN-P12 | P3 | ⬜ 计划中 | 脱敏技术支持包 | 导出系统环境、版本、连接时间线和诊断结果，并扫描排除密钥、密码、证书私钥和完整公网地址 |

### Windows 实施顺序

1. 完成 `WIN-D07` 源码拆分，为稳定性功能建立可测试边界。
2. `WIN-P01`、`WIN-P03`、`WIN-P04`、`WIN-P05`：先解决无人值守、配置回退和连接恢复。
3. `WIN-P02`：在稳定性基础完成后升级“一键检查并修复”。
4. `WIN-P06` 至 `WIN-P09`：简化值守操作和新电脑部署。
5. `WIN-P10` 至 `WIN-P12`：完成升级、远程管理和技术支持闭环。

## 共用验收原则

每项改进至少满足：

- 现有生产行为保持兼容，升级已有 VPS 或客户端时保留配置；
- 自动测试覆盖成功、失败和回退路径；
- 升级失败可以回滚，不能留下半套配置；
- 页面先说明影响哪个矿场、多少矿机、是否仍有接受 Share，再显示技术细节；
- 日志、通知和导出文件不泄露共享密钥、Webhook、矿池密码或证书私钥；
- 涉及断线风险的操作必须明确提示影响范围，并提供验证或恢复手段。

## 完成记录

每次完成后在这里追加一行，保留可审查的历史。

| 日期 | 版本 | 编号 | 结果 | 验证 |
| --- | --- | --- | --- | --- |
| 2026-09-20 | 面板 3.2.3 | VPS-D10 | 完成管理员完整说明、应急密码解释和面板入口 | 面板测试 98 项通过 |
| 2026-09-20 | 面板 3.2.2 | VPS-D03、VPS-D08 | 修复受限服务配置写入并改为局部刷新、保持页面位置 | 面板测试 97 项、服务端测试 15 项通过 |
| 2026-09-21 | Windows 2.2.2 | WIN-D07 | 中转、统计、存储与界面拆分；ecbda47 已合入 main | 完整客户端与无界面核心各 25 项、版本一致性 2 项通过 |

## 进行中阶段验收

- 2026-09-21 / WIN-P01 / 恢复策略内核：29 项策略检查通过，完整客户端及无界面核心各 25 项回归通过。尚未集成服务宿主、真实进程看门狗、持久化适配器或安装迁移；保留进行中，不视为生产可用。详见 [服务阶段说明](../secure-relay/client/src/SERVICE-DESIGN.md)。

## 发布约定与记录

- 用户于 2026-09-21 指定：每次正式发布后，将最终签名 EXE 及 SHA-256 校验文件保存在 `secure-relay/client`。所有历史正式版、兼容版和测试版均保留在原目录供随时回顾，不得因版本旧而清理；源码、脚本、图标和说明放入 src 子目录。具体规则见[客户端目录说明](../secure-relay/client/src/目录说明.md)。

- 用户于 2026-09-21 授权：后续没有特殊说明时，完成并验收通过的更新直接发布，发布后继续下一项，不再逐次请求批准。未完成阶段不得标为已完成或冒充正式功能；生产部署仍须遵循对应验证和回退要求。
- 2026-09-21：Windows 2.2.2 正式版发布于 https://github.com/JAX290/stratum-v3-relay/releases/tag/client-v2.2.2 。签名有效，远端附件 SHA-256 与本机构建一致，源码 e053f64 的完整 CI 通过。
- WIN-P01 继续：新增原子保存、备份和全局互斥适配器，15 项存储检查通过；策略 29 项与客户端 50 项回归通过。修复东八区初始空时间序列化问题。仍未接入服务宿主、进程看门狗和安装迁移。
